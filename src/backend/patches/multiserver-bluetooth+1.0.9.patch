diff --git a/node_modules/multiserver-bluetooth/index.js b/node_modules/multiserver-bluetooth/index.js
index c5ecfef..7952b56 100644
--- a/node_modules/multiserver-bluetooth/index.js
+++ b/node_modules/multiserver-bluetooth/index.js
@@ -29,16 +29,44 @@ function makePlugin(opts) {
     // Take a copy of the string
     let btInternalRepresentation = internalRepresentation.slice();
 
-    do { 
-      parts.push(btInternalRepresentation.substring(0, 2)) 
+    if(btInternalRepresentation.length < 20) {
+      do { 
+        parts.push(btInternalRepresentation.substring(0, 2)) 
+      } 
+      while( (btInternalRepresentation = btInternalRepresentation.substring(2, btInternalRepresentation.length)) != "" );  
+      
+      return parts.join(":");
+    }
+    else {
+      let splitIndex = 8;
+
+      parts.push(btInternalRepresentation.substring(0, splitIndex)) 
+
+      btInternalRepresentation = btInternalRepresentation.substring(splitIndex, btInternalRepresentation.length)
+
+      splitIndex = 4;
+
+      parts.push(btInternalRepresentation.substring(0, splitIndex)) 
+
+      btInternalRepresentation = btInternalRepresentation.substring(splitIndex, btInternalRepresentation.length)
+
+      parts.push(btInternalRepresentation.substring(0, splitIndex)) 
+
+      btInternalRepresentation = btInternalRepresentation.substring(splitIndex, btInternalRepresentation.length)
+
+      parts.push(btInternalRepresentation.substring(0, splitIndex)) 
+
+      btInternalRepresentation = btInternalRepresentation.substring(splitIndex, btInternalRepresentation.length)
+
+      parts.push(btInternalRepresentation) 
+
+      return parts.join("-");
     }
-    while( (btInternalRepresentation = btInternalRepresentation.substring(2, btInternalRepresentation.length)) != "" );
    
-    return parts.join(":");
   }
 
   function toInternalAddress(macAddress) {
-    return macAddress.split(":").join("");
+    return macAddress.split(":").join("").split("-").join("");
   }
 
   function client (address, cb) {
